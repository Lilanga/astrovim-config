return {
    'akinsho/bufferline.nvim', 
    dependencies = 'nvim-tree/nvim-web-devicons',
    -- initialize the plugin with the setup function
    config = require "user.plugins.configs.bufferline"
}